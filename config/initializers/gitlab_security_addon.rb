# frozen_string_literal: true

# GitLab Source Code Protection
# ================================
# Table auto-created on first access via ProjectSecuritySetting.ensure_table!
# No boot delay — enforcement works immediately after first admin visit.
# Model uses attribute declarations — safe even before table exists.
