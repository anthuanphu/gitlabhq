# Auto-clone proxy — /clone/* → auto-clone:3000/*
location /clone/ {
    proxy_pass http://auto-clone:3000/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_redirect http://auto-clone:3000/ /clone/;
}
